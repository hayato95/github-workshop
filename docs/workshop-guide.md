# ワークショップ進行ガイド（主催者向け）

対象: プログラミング未経験者4人 + 主催者1人
所要時間: 約4時間
目的: アプリの完成度ではなく、issue作成〜ブランチ〜実装〜PR〜レビュー〜マージ〜CIという
開発の一連の流れを体感してもらうこと。

## 事前準備（当日より前）
- [x] このリポジトリをGitHub上に作成し、push した（[hayato95/github-workshop](https://github.com/hayato95/github-workshop)）
- [ ] 4人をコラボレーターとして招待しておく（Settings > Collaborators）
- [x] 親issueを作成した（[#1](https://github.com/hayato95/github-workshop/issues/1)）
- [ ] 参加者のPCに `git` がインストールされていることを確認（`git --version`）

**sub-issueは当日、参加者自身が作成します**（事前には作らない）。これは「課題を
sub-issueに切り分ける」体験そのものがワークショップのゴールの1つだからです。
`docs/issues.md` に、参加者が参考にできる仕様の下書きを用意してあります。

## 当日タイムテーブル（4時間 / 240分）

| 時間 | 内容 |
|------|------|
| 0:00–0:20 (20分) | 導入: なぜgit/GitHubを使うのか、ブランチとは何か（`docs/git-cheatsheet.md`を使って説明） |
| 0:20–0:35 (15分) | 環境確認: `git clone`、リポジトリの中身を一緒に確認 |
| 0:35–1:00 (25分) | issue分解タイム: 親issue #1 を読み、4人で1人1関数ずつ担当できるようsub-issueを4件作成する（`docs/issues.md`を参考にしてよい）。GitHubの「Create sub-issue」機能を使う |
| 1:00–1:15 (15分) | 作業ブランチの作成（`git checkout -b ...`） |
| 1:15–2:15 (60分) | 実装タイム: 各自 `app-logic.js` の担当関数を実装。詰まったらAIに相談してOK |
| 2:15–2:35 (20分) | commit & push、Pull Requestの作成 |
| 2:35–3:10 (35分) | レビュータイム: ペアや全体でお互いのPRをレビュー・コメント・Approve |
| 3:10–3:30 (20分) | マージ: 順番にmainへマージし、GitHub Actionsのテスト結果を確認 |
| 3:30–3:45 (15分) | 動作確認: 全員の実装がそろった `index.html` をブラウザで開いて遊んでみる |
| 3:45–4:00 (15分) | 振り返り: 感想共有、つまずいた点の共有 |

## マージ順のすすめ方
4人とも同じ `app-logic.js` を編集するため、マージの順番次第では軽微なコンフリクトが起きることがあります。
- 基本方針: PRが早く準備できた人から順にマージしてOK（4関数はファイル内で離れた場所にあるため、通常は自動でマージされます）
- コンフリクトが発生した場合: 慌てず「学びのチャンス」として一緒に解消する（`docs/git-cheatsheet.md`の`git merge main`の手順を参照）
- 担当D（`getLuckyMessage`）は担当A（`getFortune`）の返り値の文字列に依存しているため、可能なら担当Aを先にマージすると混乱が少ない

## 進行のコツ
- 実装そのものよりも「git/GitHubの操作を1人でひと通り経験できたか」を優先する
- 詰まっている参加者がいたら、`git status` の出力を見せてもらいながら一緒に状況を確認する
- レビューは「間違い探し」ではなく「一緒に確認する」という空気づくりを意識する
- 時間が押した場合は、レビュー時間を短縮してでもマージ〜CI確認〜動作確認までは必ず到達する

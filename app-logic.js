// おみくじアプリのロジック関数群
// ブラウザ(script.js)からも、Node.jsのテスト(tests/app-logic.test.js)からも
// 同じ関数を使えるように、下部でmodule.exportsのガードをしている。

// ------------------------------------------------------------
// サンプル関数（実装済み・パターンの見本）
// index (0〜4の整数) を受け取り、対応する値を配列から返すだけの関数。
// 担当issueの4関数も、基本はこれと同じ考え方で実装できる。
// ------------------------------------------------------------
function getLuckyNumber(index) {
  const LUCKY_NUMBERS = [7, 3, 21, 4, 9];
  return LUCKY_NUMBERS[index];
}

// ------------------------------------------------------------
// 担当A: 運勢を判定する
// index (0〜4の整数) を受け取り、運勢ラベルを返す関数を実装してください。
//
// 対応表:
//   0 -> "大吉"
//   1 -> "中吉"
//   2 -> "吉"
//   3 -> "小吉"
//   4 -> "凶"
//
// 例: getFortune(0) は "大吉" を返す
// ------------------------------------------------------------
function getFortune(index) {
  throw new Error('TODO: 実装してください (担当A: getFortune)');
}

// ------------------------------------------------------------
// 担当B: ラッキーカラーを決める
// index (0〜4の整数) を受け取り、ラッキーカラーを返す関数を実装してください。
//
// 対応表:
//   0 -> "赤"
//   1 -> "青"
//   2 -> "黄"
//   3 -> "緑"
//   4 -> "紫"
//
// 例: getLuckyColor(0) は "赤" を返す
// ------------------------------------------------------------
function getLuckyColor(index) {
  throw new Error('TODO: 実装してください (担当B: getLuckyColor)');
}

// ------------------------------------------------------------
// 担当C: ラッキーアイテムを決める
// index (0〜4の整数) を受け取り、ラッキーアイテムを返す関数を実装してください。
//
// 対応表:
//   0 -> "傘"
//   1 -> "腕時計"
//   2 -> "本"
//   3 -> "コーヒー"
//   4 -> "スニーカー"
//
// 例: getLuckyItem(0) は "傘" を返す
// ------------------------------------------------------------
function getLuckyItem(index) {
  throw new Error('TODO: 実装してください (担当C: getLuckyItem)');
}

// ------------------------------------------------------------
// 担当D: 運勢別の一言メッセージを決める
// fortune (getFortuneが返す文字列: "大吉"/"中吉"/"吉"/"小吉"/"凶") を受け取り、
// 応援メッセージを返す関数を実装してください。
//
// 対応表:
//   "大吉" -> "最高の1日になりそう！"
//   "中吉" -> "良いことがありそうな予感！"
//   "吉"   -> "いつも通り、落ち着いて過ごそう"
//   "小吉" -> "小さな幸せを見つけよう"
//   "凶"   -> "無理せず、ゆっくり過ごそう"
//
// 例: getLuckyMessage("大吉") は "最高の1日になりそう！" を返す
// 注意: 担当Aが実装するgetFortoneの返り値の文字列と一致させる必要があります。
// ------------------------------------------------------------
function getLuckyMessage(fortune) {
  throw new Error('TODO: 実装してください (担当D: getLuckyMessage)');
}

// ブラウザとNode.js(テスト)の両方から使えるようにするためのガード
if (typeof module !== 'undefined' && module.exports) {
  module.exports = {
    getLuckyNumber,
    getFortune,
    getLuckyColor,
    getLuckyItem,
    getLuckyMessage,
  };
}
